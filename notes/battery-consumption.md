# Battery Consumption

Observations running Iris on a Mac Mini (battery-powered, no monitor).

## 2026-02-14

- **96% → 81%** over several hours (as of 12:44 PM EST) with continuous mic listening + periodic camera captures
- Visual mode active, `caffeinate -d` / `pmset disablesleep` keeping system awake
- Drain to 10% pending — will update with total runtime
